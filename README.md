# Dev Server

Update your env file with correct backend url
```
VITE_API_URL=http://localhost:3000
```

`yarn`
or
`npm i`

start

`yarn dev`
or
`npm run dev`

# Build for prod
`yarn build`
or
`npm run build`


# Preview Build
`yarn serve`
or 
`npm run serve`



